<% if signed_in? %>
	<%= render 'shared/signed_in_homepages' %>
<% else %>
	<%= render 'shared/non_signed_in_homepages' %>
	<%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary"%>
	<%= link_to image_tag("rails.png", alt:"Rails"), 'https://rubyonrails.org'%>
<% end %>
